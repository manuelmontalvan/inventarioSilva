// lib/api/productSales.ts
/*import axios from '@/lib/axiosInstance';
import { ProductSaleI } from '@/types/productSale';

export const getProductSales = async (): Promise<ProductSaleI[]> => {
  const res = await axios.get('/product-sales');
  return res.data;
};

export const getProductSaleById = async (id: string): Promise<ProductSaleI> => {
  const res = await axios.get(`/product-sales/${id}`);
  return res.data;
};

export const createProductSale = async (sale: Partial<ProductSaleI>): Promise<ProductSaleI> => {
  const res = await axios.post('/product-sales', sale);
  return res.data;
};

export const updateProductSale = async (id: string, sale: Partial<ProductSaleI>): Promise<ProductSaleI> => {
  const res = await axios.patch(`/product-sales/${id}`, sale);
  return res.data;
};

export const deleteProductSale = async (id: string): Promise<void> => {
  await axios.delete(`/product-sales/${id}`);
};*/
